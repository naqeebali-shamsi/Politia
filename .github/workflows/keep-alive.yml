name: Keep Backend Alive
on:
  schedule:
    - cron: '*/14 * * * *'
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render backend
        run: curl -sf https://politia-api.onrender.com/health || echo "Backend warming up"
